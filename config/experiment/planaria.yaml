# @package _global_
experiment: planaria

dataset:
  _target_: noise2same.dataset.PlanariaDataset
  path: ${hydra.runtime.cwd}/data/Denoising_Planaria/
  n_dim: 3
  transforms:
    _target_: noise2same.dataset.util.training_augmentations_3d

dataset_valid:
  mode: val

training:
  steps: 50000
  crop: 64
  batch_size: 4