# @package _global_
experiment: bsd68
data:
  _target_: noise2same.dataset.BSD68Dataset
  path: data/BSD68/
  n_dim: 2

training:
  crop: 128  # 64 in the paper
  steps: 80000
  batch_size: 32  # 64 in the paper

model:
  lambda_inv: 0.95
