# @package _global_
experiment: bsd68

dataset:
  _target_: noise2same.dataset.BSD68Dataset
  path: ${cwd}/data/BSD68/

dataset_train:
  mode: train
  transforms:
    _target_: noise2same.dataset.util.training_augmentations_2d
    crop: 128  # 64 in Noise2Same paper
  input_size: ${.transforms.crop}  # needed for some models

dataset_valid:
  mode: val

dataset_test:
  mode: test

training:
  steps: 80000
  batch_size: 32  # 64 in Noise2Same paper

# For Noise2Same
#denoiser:
#  lambda_inv: 0.95
