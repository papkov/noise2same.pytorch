# @package _global_
experiment: synthetic_grayscale

dataset:
  _target_: noise2same.dataset.BSD400SyntheticDataset
  path: ${hydra.runtime.cwd}/data/BSD400/
  noise_type: gaussian
  noise_param: 25
  standardize: True
  transforms:
    _target_: noise2same.dataset.util.training_augmentations_2d
    crop: 64

dataset_valid:
  _target_: noise2same.dataset.BSD68SyntheticDataset
  path: ${hydra.runtime.cwd}/data/BSD68-test/
  fixed: True
  transforms:
    _target_: noise2same.dataset.util.validation_transforms_2d
    crop: 64

dataset_test:
  _target_: noise2same.dataset.SyntheticTestDataset
  # Top-level arguments override partial arguments
  transforms: null
  datasets:
    - _target_: noise2same.dataset.BSD68SyntheticDataset
      _partial_: True
      path: ${hydra.runtime.cwd}/data/BSD68-test/
      n_repeats: 4
    - _target_: noise2same.dataset.Set12SyntheticDataset
      _partial_: True
      path: ${hydra.runtime.cwd}/data/Set12/
      n_repeats: 20

training:
  crop: 64
  steps: 50000
  batch_size: 64
  validate: True
