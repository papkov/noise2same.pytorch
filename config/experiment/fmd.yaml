# @package _global_
experiment: fmd

dataset:
  _target_: noise2same.dataset.FMDDataset
  path: data/FMD/
  part: cf_fish
  add_blur_and_noise: False

dataset_valid:
  mode: val

training:
  crop: 64
  steps: 20000
  batch_size: 64
  monitor: val_mse

optim:
  weight_decay: 1e-8